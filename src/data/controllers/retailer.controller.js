'use strict';
import { RetailerDAO } from '../daos/retailer.dao';
export class RetailerController {
    constructor() {
        this.async = store(req, string, res, string);
    }
}
{
    let _retailerDAO = new RetailerDAO();
    let retailer = await, _retailerDAO, store = ();
    res.json(retailer);
}
//# sourceMappingURL=retailer.controller.js.map